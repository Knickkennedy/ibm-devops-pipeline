{{- if .Values.frontend.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "devops-platform.serviceAccountName" (dict "componentName" "frontend" "context" .) }} 
  labels:
    {{- include "devops-platform.labels" (dict "componentName" "frontend" "context" .) | nindent 4 }}
    {{- with .Values.frontend.serviceAccount.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.frontend.serviceAccount.annotations }}
  annotations :
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false 
{{- end }}
