apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-platform.componentFullname" (dict "componentName" "frontend" "context" .) }}
  labels:
    {{- include "devops-platform.labels" (dict "componentName" "frontend" "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-platform.selectorLabels" (dict "componentName" "frontend" "context" .) | nindent 4 }}

