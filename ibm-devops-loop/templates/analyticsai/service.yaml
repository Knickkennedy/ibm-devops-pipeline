apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-platform.componentFullname" (dict "componentName" "analyticsai" "context" .) }}
  labels:
    {{- include "devops-platform.labels" (dict "componentName" "analyticsai" "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.analyticsai.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-platform.selectorLabels" (dict "componentName" "analyticsai" "context" .) | nindent 4 }}

