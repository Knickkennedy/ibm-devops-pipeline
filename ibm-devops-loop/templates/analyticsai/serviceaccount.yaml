{{- if .Values.analyticsai.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "devops-platform.serviceAccountName" (dict "componentName" "analyticsai" "context" .) }} 
  labels:
    {{- include "devops-platform.labels" (dict "componentName" "analyticsai" "context" .) | nindent 4 }}
    {{- with .Values.analyticsai.serviceAccount.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.analyticsai.serviceAccount.annotations }}
  annotations :
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false 
{{- end }}
