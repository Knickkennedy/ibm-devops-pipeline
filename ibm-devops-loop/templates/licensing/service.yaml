apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-platform.componentFullname" (dict "componentName" "licensing" "context" .) }}
  labels:
    {{- include "devops-platform.labels" (dict "componentName" "licensing" "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.licensing.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-platform.selectorLabels" (dict "componentName" "licensing" "context" .) | nindent 4 }}

