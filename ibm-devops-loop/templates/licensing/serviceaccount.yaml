{{- if .Values.licensing.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "devops-platform.serviceAccountName" (dict "componentName" "licensing" "context" .) }} 
  labels:
    {{- include "devops-platform.labels" (dict "componentName" "licensing" "context" .) | nindent 4 }}
    {{- with .Values.licensing.serviceAccount.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.licensing.serviceAccount.annotations }}
  annotations :
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false 
{{- end }}
