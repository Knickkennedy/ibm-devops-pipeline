apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-platform.componentFullname" (dict "componentName" "tenant" "context" .) }}
  labels:
    {{- include "devops-platform.labels" (dict "componentName" "tenant" "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.tenant.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-platform.selectorLabels" (dict "componentName" "tenant" "context" .) | nindent 4 }}

