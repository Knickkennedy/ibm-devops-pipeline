{{- if .Values.tenant.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "devops-platform.serviceAccountName" (dict "componentName" "tenant" "context" .) }} 
  labels:
    {{- include "devops-platform.labels" (dict "componentName" "tenant" "context" .) | nindent 4 }}
    {{- with .Values.tenant.serviceAccount.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.tenant.serviceAccount.annotations }}
  annotations :
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false 
{{- end }}
