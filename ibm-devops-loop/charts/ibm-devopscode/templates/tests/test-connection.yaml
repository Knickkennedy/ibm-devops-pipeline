apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-code-client.fullname" . }}-test-connection"
  labels:
    {{- include "devops-code-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-code-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
