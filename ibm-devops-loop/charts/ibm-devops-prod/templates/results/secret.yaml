apiVersion: v1
kind: Secret
metadata:
  labels:
    app.kubernetes.io/instance: '{{ .Release.Name }}'
    app.kubernetes.io/managed-by: '{{ .Release.Service }}'
    app.kubernetes.io/name: results
    app.kubernetes.io/part-of: '{{ .Chart.Name }}'
    app.kubernetes.io/version: '{{ regexReplaceAll "-.*" .Chart.AppVersion "" }}'
    helm.sh/chart: '{{ .Chart.Name }}-{{ .Chart.Version }}'
  name: '{{ .Release.Name }}-results'
type: Opaque
data:
{{- if include "testhub.results.enabled" . }}
{{- if .Values.results.existingOauthClientSecret }}
  oauth-client-secret: {{ .Values.results.existingOauthClientSecret | b64enc | quote }}
{{- else }}
  oauth-client-secret: {{ print (default .Release.Name (default .Values.global.passwordSeed .Values.passwordSeed)) "results-oauth-client-secret" | sha256sum | b64enc | quote }}
{{- end }}
{{- end }}
{{- if .Values.results.existingPostgresqlPassword }}
  postgresql-password: {{ .Values.results.existingPostgresqlPassword | b64enc | quote }}
{{- else }}
  postgresql-password: {{ print (default .Release.Name (default .Values.global.passwordSeed .Values.passwordSeed)) "results-postgresql" | sha256sum | b64enc | quote }}
{{- end }}
