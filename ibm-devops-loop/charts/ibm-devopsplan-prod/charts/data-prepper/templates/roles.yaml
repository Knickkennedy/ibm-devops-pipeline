{{- if .Values.global.platform.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ template "ccm.subchart.fullname" . }}-data-prepper
  labels:
    {{- include "data-prepper.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list"]
{{- end -}}