{{- if (.Capabilities.APIVersions.Has "route.openshift.io/v1") }}
{{- include "sch.config.init" (list . "ibm-ucd-prod.sch.chart.config.values") -}}
{{- $fullName := include "sch.names.fullName" (list .) -}}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ $fullName }}-wss
  labels:
{{ include "sch.metadata.labels.standard" (list . "route") | indent 4 }}
spec:
  {{- if .Values.ingress.wsshost }}
  host: {{ .Values.ingress.wsshost | quote }}
  {{- else if eq (((.Values.global).platform).enabled) (true) }}
  host: "{{.Values.global.serviceDomainPrefix}}{{ .Values.global.domain }}"
  {{- end }}
  subdomain: ''
  to:
    kind: Service
    name: {{ $fullName | quote }}
    weight: 100
  port:
    targetPort: wss
  tls:
    termination: passthrough
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
status:
  ingress: []
{{- end }}
