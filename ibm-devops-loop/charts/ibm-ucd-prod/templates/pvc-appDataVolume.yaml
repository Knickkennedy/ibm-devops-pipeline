{{ if and .Values.persistence.enabled (not .Values.appDataVolume.existingClaimName) -}}
{{- include "sch.config.init" (list . "ibm-ucd-prod.sch.chart.config.values") -}}
{{- $fullName := include "sch.names.fullName" (list .) -}}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ $fullName }}-{{ .Values.appDataVolume.name }}
  labels:
{{ include "sch.metadata.labels.standard" (list . "") | indent 4 }}
spec:
  {{- if .Values.persistence.useDynamicProvisioning }}
  ## if present, use the storageClassName from the values.yaml, else use the
  ## default storageClass setup by kube Administrator
  ##
  ## setting storageClassName to nil means use the default storage class
  {{- if .Values.appDataVolume.storageClassName }}
  storageClassName: {{ .Values.appDataVolume.storageClassName | quote }}
  {{- else if and (((.Values.global).platform).enabled) (((.Values.global).persistence).rwoStorageClass) }}
  storageClassName: {{ .Values.global.persistence.rwoStorageClass | quote }}
  {{- end }}
  {{- else }}
  ## disable dynamic provisioning
  storageClassName: ""
  {{- end }}

  accessModes:
    - {{ .Values.appDataVolume.accessMode }}
  resources:
    requests:
      storage: {{ .Values.appDataVolume.size | quote }}
{{- end }}
