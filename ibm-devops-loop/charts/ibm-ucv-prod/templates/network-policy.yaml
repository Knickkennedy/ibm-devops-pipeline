apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default
  labels:
    {{- include "ucv.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app: velocity
  ingress:
  - {}
